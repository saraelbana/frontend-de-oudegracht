export const createRequestData = ({ firstname, lastname, email, username, password }) => {
    return {

        firstname: firstname,
        lastname: lastname,
        email: email,
        username: username,
        password: password
    };
};