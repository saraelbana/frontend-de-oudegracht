export const createRequestData = ({ firstname, lastname, email, username, password, phone, role }) => {
    return {
        firstname,
        lastname,
        email,
        username,
        password,
        phone,
        role
    };
};

